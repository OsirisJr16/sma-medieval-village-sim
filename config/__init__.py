"""Configuration layer.

Centralizes all tunable parameters, constants, and the color palette so that
the rest of the codebase never hard-codes magic values. Import the shared
settings singleton via :func:`config.settings.get_settings`.
"""
