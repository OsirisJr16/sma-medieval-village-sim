"""World / environment package.

Models the simulated environment the agents inhabit: the spatial map, terrain,
climate (weather and seasons), and world resource nodes. The :class:`world.world.World`
aggregate is the single entry point the model uses to query and mutate the
environment.
"""
